// Run: npx ts-node --project tsconfig.json scripts/seed.ts
// Or via: node -e "require('./scripts/seed.js')"
// Better: Add a Next.js API route /api/seed for one-time setup

// This script seeds the initial admin user and demo profile data.
// Usage: run via /api/admin/seed (protected, one-time use)
